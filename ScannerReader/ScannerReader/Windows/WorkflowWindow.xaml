<Window x:Class="ScannerReader.Windows.WorkflowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScannerReader.Windows"
        mc:Ignorable="d"
        Loaded="WorkflowWindow_OnLoaded"
        KeyDown="WorkflowWindow_OnKeyDown"
        Title="WorkflowWindow" Height="520.059" Width="1076.544" WindowState="Maximized" WindowStyle="None">
    <Grid>
        <DockPanel LastChildFill="true" Margin="0,0,0,0">
            <StackPanel DockPanel.Dock="Top">
                <TextBlock Text="{Binding Message, Mode=OneWay}"  Height="35" Margin="0,0" FontSize="25" Padding="10px, 0" Background="Yellow" Foreground="#FFFF1818"></TextBlock>
                <StackPanel Orientation="Horizontal" Height="35">
                    
                </StackPanel>
            </StackPanel>
            <DockPanel LastChildFill="true">
                <TextBlock Text="{Binding Comment, Mode=OneWay}" DockPanel.Dock="Left" Width="300px" Padding="10px" Background="LightGray"></TextBlock>
                <Image DockPanel.Dock="Right"></Image>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
